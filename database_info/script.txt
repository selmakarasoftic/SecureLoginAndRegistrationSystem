SHOW TABLES;


CREATE TABLE users (
  id INT AUTO_INCREMENT PRIMARY KEY,
  full_name VARCHAR(100) NOT NULL,
  email VARCHAR(255) NOT NULL UNIQUE,
  username VARCHAR(30) NOT NULL UNIQUE,
  password_hash VARCHAR(255) NOT NULL,
  phone VARCHAR(20) NOT NULL,
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);


DESCRIBE users;

DESCRIBE users;
SELECT * FROM users;


ALTER TABLE users
ADD COLUMN two_factor_secret VARCHAR(255) NULL,
ADD COLUMN two_factor_enabled BOOLEAN DEFAULT FALSE;

ALTER TABLE users
ADD UNIQUE (phone);

CREATE TABLE reserved_usernames (
  id INT AUTO_INCREMENT PRIMARY KEY,
  username VARCHAR(50) UNIQUE NOT NULL
);

INSERT INTO reserved_usernames (username) VALUES
('admin'),
('root'),
('system'),
('support');

ALTER TABLE users
ADD COLUMN email_verified BOOLEAN DEFAULT FALSE;

CREATE TABLE email_verification_tokens (
  id INT AUTO_INCREMENT PRIMARY KEY,
  user_id INT NOT NULL,
  token_hash VARCHAR(255) NOT NULL,
  expires_at DATETIME NOT NULL,
  used_at DATETIME NULL,
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  FOREIGN KEY (user_id) REFERENCES users(id)
);

ALTER TABLE users ADD COLUMN is_blocked BOOLEAN DEFAULT FALSE;

DESCRIBE users;
DESCRIBE email_verification_tokens;
describe reserved_usernames;


CREATE TABLE two_factor_codes (
  id INT AUTO_INCREMENT PRIMARY KEY,
  user_id INT NOT NULL,
  method ENUM('email', 'sms') NOT NULL,
  code_hash VARCHAR(255) NOT NULL,
  expires_at DATETIME NOT NULL,
  used_at DATETIME NULL,
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  FOREIGN KEY (user_id) REFERENCES users(id)
);

CREATE TABLE recovery_codes (
  id INT AUTO_INCREMENT PRIMARY KEY,
  user_id INT NOT NULL,
  code_hash VARCHAR(255) NOT NULL,
  used_at DATETIME NULL,
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  FOREIGN KEY (user_id) REFERENCES users(id)
);

select * from users


CREATE TABLE IF NOT EXISTS login_2fa_tokens (
  id INT AUTO_INCREMENT PRIMARY KEY,
  user_id INT NOT NULL,
  token_hash VARCHAR(255) NOT NULL,
  expires_at DATETIME NOT NULL,
  used_at DATETIME NULL,
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  FOREIGN KEY (user_id) REFERENCES users(id)
);

CREATE TABLE IF NOT EXISTS two_factor_codes (
  id INT AUTO_INCREMENT PRIMARY KEY,
  user_id INT NOT NULL,
  method ENUM('email', 'sms') NOT NULL,
  code_hash VARCHAR(255) NOT NULL,
  expires_at DATETIME NOT NULL,
  used_at DATETIME NULL,
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  FOREIGN KEY (user_id) REFERENCES users(id)
);

CREATE TABLE IF NOT EXISTS recovery_codes (
  id INT AUTO_INCREMENT PRIMARY KEY,
  user_id INT NOT NULL,
  code_hash VARCHAR(255) NOT NULL,
  used_at DATETIME NULL,
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  FOREIGN KEY (user_id) REFERENCES users(id)
);

ALTER TABLE users
ADD COLUMN two_factor_method ENUM('email', 'sms', 'totp') NULL;



DELETE FROM recovery_codes
WHERE user_id = 4;

UPDATE users
SET two_factor_enabled = FALSE,
    two_factor_method = NULL,
    two_factor_secret = NULL
WHERE id = 4;

CREATE TABLE IF NOT EXISTS refresh_tokens (
  jti VARCHAR(255) PRIMARY KEY,
  user_id INT NOT NULL,
  issued_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  expires_at DATETIME NOT NULL,
  revoked_at DATETIME NULL,
  replaced_by VARCHAR(255) NULL,
  user_agent TEXT NULL,
  ip VARCHAR(100) NULL,

  CONSTRAINT fk_refresh_tokens_user
    FOREIGN KEY (user_id)
    REFERENCES users(id)
    ON DELETE CASCADE
);

select * from
    refresh_tokens;

CREATE TABLE IF NOT EXISTS audit_logs (
  id INT AUTO_INCREMENT PRIMARY KEY,
  actor_id INT NULL,
  actor_role VARCHAR(50) NULL,
  action VARCHAR(100) NOT NULL,
  object VARCHAR(100) NULL,
  object_id VARCHAR(100) NULL,
  original_object JSON NULL,
  request_data JSON NULL,
  ip_address VARCHAR(100) NULL,
  user_agent TEXT NULL,
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,

  INDEX idx_audit_action_created_at (action, created_at),
  INDEX idx_audit_actor_created_at (actor_id, created_at),
  INDEX idx_audit_ip_created_at (ip_address, created_at)
);

select  * from audit_logs;
select * from users;

ALTER TABLE users
ADD COLUMN role ENUM('user', 'admin') NOT NULL DEFAULT 'user';

select * from users;

CREATE TABLE IF NOT EXISTS password_reset_tokens (
  id INT AUTO_INCREMENT PRIMARY KEY,
  user_id INT NOT NULL,
  token_hash VARCHAR(255) NOT NULL UNIQUE,
  expires_at DATETIME NOT NULL,
  used_at DATETIME NULL,
  attempt_count INT NOT NULL DEFAULT 0,
  attempt_window_start DATETIME NULL,
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,

  INDEX idx_password_reset_user_created (user_id, created_at),
  INDEX idx_password_reset_token_hash (token_hash),
  INDEX idx_password_reset_expires (expires_at),

  CONSTRAINT fk_password_reset_user
    FOREIGN KEY (user_id)
    REFERENCES users(id)
    ON DELETE CASCADE
);


CREATE TABLE IF NOT EXISTS trusted_devices (
  id INT AUTO_INCREMENT PRIMARY KEY,
  user_id INT NOT NULL,
  device_hash VARCHAR(255) NOT NULL,
  user_agent TEXT NULL,
  ip_address VARCHAR(100) NULL,
  expires_at DATETIME NOT NULL,
  revoked_at DATETIME NULL,
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,

  INDEX idx_trusted_user (user_id),
  INDEX idx_trusted_device_hash (device_hash),
  INDEX idx_trusted_expires (expires_at),

  CONSTRAINT fk_trusted_devices_user
    FOREIGN KEY (user_id)
    REFERENCES users(id)
    ON DELETE CASCADE
);


CREATE TABLE IF NOT EXISTS sessions (
  id INT AUTO_INCREMENT PRIMARY KEY,
  user_id INT NOT NULL,
  trusted_device_id INT NULL,
  user_agent TEXT NULL,
  ip_address VARCHAR(100) NULL,
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  expires_at DATETIME NOT NULL,
  revoked_at DATETIME NULL,

  INDEX idx_sessions_user (user_id),
  INDEX idx_sessions_trusted_device (trusted_device_id),
  INDEX idx_sessions_expires (expires_at),

  CONSTRAINT fk_sessions_user
    FOREIGN KEY (user_id)
    REFERENCES users(id)
    ON DELETE CASCADE,

  CONSTRAINT fk_sessions_trusted_device
    FOREIGN KEY (trusted_device_id)
    REFERENCES trusted_devices(id)
    ON DELETE SET NULL
);

ALTER TABLE refresh_tokens
ADD COLUMN session_id INT NULL;

ALTER TABLE refresh_tokens
ADD INDEX idx_refresh_tokens_session (session_id);

ALTER TABLE refresh_tokens
ADD CONSTRAINT fk_refresh_tokens_session
FOREIGN KEY (session_id)
REFERENCES sessions(id)
ON DELETE CASCADE;

DELETE FROM audit_logs
WHERE action IN ('LOGIN_FAILED', 'LOGIN_CAPTCHA_FAILED', 'LOGIN_CAPTCHA_PASSED');